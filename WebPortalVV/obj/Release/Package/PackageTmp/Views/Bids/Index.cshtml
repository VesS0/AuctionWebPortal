@model IEnumerable<WebPortalVV.Models.Bid>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.timeSent)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.timeSent)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idBid }) |
            @Html.ActionLink("Details", "Details", new { id=item.idBid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idBid })
        </td>
    </tr>
}

</table>
